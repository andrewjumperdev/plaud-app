import BlogPage from '../components/BlogPage';

export const metadata = {
  title: 'Blog — Plaud Argentina',
  description:
    'Casos, estrategias y reflexiones sobre cómo las empresas más inteligentes documentan cada conversación con Plaud.',
};

export default function Blog() {
  return <BlogPage />;
}
